/**
 * Mock data removed. Use API endpoints instead.
 * Kept placeholder exports so legacy imports don't break during the transition.
 */

export const mockItems = [];
export const mockCategories = [];
export const mockListings = [];
export const mockSeller = {};
export const mockFilters = { categories: [], priceRanges: [], ratings: [] };
